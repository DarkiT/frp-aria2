FROM alpine
MAINTAINER ZiShuo <www@zishuo.uu.me>

ARG TZ="Asiz/Shanghai"
ENV TZ ${TZ}

RUN sed -i 's/dl-cdn.alpinelinux.org/mirrors.ustc.edu.cn/g' /etc/apk/repositories && \
	apk add --update \
	bash \
	wget \
	tar \
	echo "${TZ}" > /etc/timezone && \
	ln -sf /usr/share/zoneinfo/${TZ} /etc/localtime && \
	rm -rf /var/cache/apk/*

CMD ["/bin/bash"]